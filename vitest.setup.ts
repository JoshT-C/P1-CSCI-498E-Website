import { initTestEnvironment } from '@angular/core/testing';
import { BrowserTestingModule } from '@angular/platform-browser/testing';

const result = initTestEnvironment(BrowserTestingModule, undefined);
